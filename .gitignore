node_modules/
dist/
.next/
out/
*.tsbuildinfo
coverage/
.turbo/

# Secrets — never commit. See SECURITY.md.
.env
.env.*
!.env.example

# Credential notes for a human or a handoff. CLAUDE.md is the public-safe context
# file; anything ending .local.md may contain passwords and must never be pushed.
*.local.md

# Local data
pgdata/
*.log
.DS_Store
Thumbs.db

# Checkpoint transcripts are generated artefacts, not source.
checkpoint-*.txt

# Next.js build output and Playwright artefacts.
.next/
apps/web/test-results/
apps/web/playwright-report/
