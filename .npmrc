# Keep the dependency graph honest: a package must declare what it imports.
# This is what makes the module boundaries in ARCHITECTURE.md 4.1 enforceable.
strict-peer-dependencies=false
auto-install-peers=true
# vitest's bundler needs its postinstall to produce a usable binary.
enable-pre-post-scripts=true
